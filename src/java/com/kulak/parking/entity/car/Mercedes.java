package com.kulak.parking.entity.car;

public class Mercedes extends Car {

    public Mercedes(double pricePerHour, double fuelConsumptionPerTenKm) {
        super(pricePerHour, "Mercedes", fuelConsumptionPerTenKm);
    }
}
